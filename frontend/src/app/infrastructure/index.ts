/**
 * Infrastructure layer: HTTP, repositories, implementations of domain interfaces.
 * Future: central de ações (Command pattern) for all backend requests.
 */

export const INFRASTRUCTURE_PLACEHOLDER = true;
